services:
  backend:
    build:
      context: ./backend
    image: careerpath-backend:latest
    container_name: careerpath-backend
    ports:
      - "8010:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      USE_LOCAL_EMBEDDINGS: "true"
      CHROMA_HOST: "chroma"
      CHROMA_PORT: "8000"
      # HF_TOKEN is required for /chat RAG, /roadmap, /interview/*, and the
      # LLM-augmented /summarize-cv. Set it in a .env file next to this
      # compose file (docker-compose reads it automatically).
      HF_TOKEN: ${HF_TOKEN:-}
    depends_on:
      - chroma
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  frontend:
    build:
      context: ./frontend
      args:
        # Frontend is built statically; this URL is baked into the bundle.
        # Point it at the backend service name when both run in this stack,
        # or override to a public URL for production builds.
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8010}
    image: careerpath-frontend:latest
    container_name: careerpath-frontend
    # Host port 18080 -> container nginx :80. Avoid 5173/8080 clashes.
    ports:
      - "18080:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8002:8000"
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

volumes:
  chroma_data:
